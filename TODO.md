# TODO

## Blocks v1

- [ ] **Diff viewer** — press enter on a file in stage tab to see what changed before committing
- [ ] **Scrolling** — fixed-height viewport for long branch/commit/stash lists
- [ ] **No-repo error handling** — detect and exit cleanly when run outside a git repo

## Important

- [ ] **`gitty commit` staged check** — warn if nothing is staged
- [ ] **Branch creation** — create a new branch from the branch tab
- [ ] **Amend** — `git commit --amend` support

## Polish

- [ ] **Long path truncation** — deep paths overflow the stage tab layout
- [ ] **Commitlint validation** — wire up `@commitlint/lint` in the commit form
